1) Лексикографска сортировка на масив от числа
2) Тройчко дърво със стойност букви
    - да се издеве думата на дъното, като трябва да са с една и съща дължина
    - дървета и файл
3) ФП - сумата на минималните елементи,за които f(x) = x
4) SQL - да се напишат две заявки
    - да се изведе името на студиото, 
                   най-ранна година на техен филм, 
                   най-голяма година на техен филм,
                   брой филми за студиото
                за студиота чието име започва с 'М'
    - да се изведе името на актрисата,
                    брой филми в които е учствала
                за актриса с най-голям брой филми.
5) BASH -- SHIT
6) C++ може да се ползва стандартната библиотека
    да се намише клас Polinom:
                    int stepen;
                    std::vector<int> coefs;
    И да се преденифират следните оператори:
        operator-, operator+ (Polinom& p1, Polinom& p2)
         T operator[](int index) --> return the coef on given index
         T operator()(int x) --> да се сметне стойността на полинома при определена точка
         Polinom& operator*(T coef) ----> да се умножи в всички коефециенти
         operator+=, 
        operator-=, operator*=
7) ДЦ - оцветяване на граф ...
8) ДИС - интеграл(dx/(2 + sin x))
